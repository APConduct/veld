import std.result.{Result}

# Test the most minimal failing case
let ok_result: Result<i32, str> = Result.Ok(42)

# This should work but fails with "Type any does not have methods"
let mapped = ok_result.map(x => x * 2)

# Try to use mapped - this is where it fails
mapped.unwrap_or(999)
