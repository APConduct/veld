import std.vec.{Vec}

# Test 1: Create empty Vec and check initial state
var vec = Vec.new()
let initial_length = vec.len()        # Should be 0 - this should work

# Test 2: Check if push works
vec.push(42)
let length_after_first_push = vec.len()       # Should be 1 - this might fail

length_after_first_push
