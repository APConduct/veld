# Test: Simple for loop with tuple destructuring

let pairs = [(1, 2)]

std.io.println("Testing for loop with single tuple")
for (x, y) in pairs do
    std.io.println("x = " + x.to_str() + ", y = " + y.to_str())
end

std.io.println("Test completed!")
