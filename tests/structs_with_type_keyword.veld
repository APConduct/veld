# Plex definition
type Point = { x: f64, y: f64 } # Structural because of the '{...}'

# Struct def
type PointStruct = |{ x: f64, y: f64 } # nominal because of the '|' before the '{...}'

type EitherUnion<T, U> = T | U

type Either<T, U> =
    | Left of T
    | Right of U
