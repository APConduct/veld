pub struct Foo<T>
    pub t: T
end

impl<T> Foo<T> <- std.ToString if T: std.ToString then
    pub fn to_string(self) -> str
        return self.t.to_string()
    end
end

pub fn main()
    let foo = Foo<str> { t: "hello" }
    assert~(foo.to_string() == "hello")
end

main()
