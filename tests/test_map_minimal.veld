#| Minimal test for Option.map with generic method parameters

import std.option.{Option}
import std.io.{println}

println("Testing Option.map...")

let opt = Option.Some(42)
let doubled = opt.map(fn (x) -> x * 2 end)

println("Done")
